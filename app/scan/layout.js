export const metadata = {
  title: 'Scan Safaricom Logo | Safaricom AR Zone',
  description: 'Point camera at the Safaricom logo to unlock instant games.',
};

export default function ScanLayout({ children }) {
  return children;
}
