export const metadata = {
  title: 'Event Admin Portal | Safaricom AR Zone',
  description: 'Manage physical target logo compilation and recommendation game links.',
};

export default function EventAdminLayout({ children }) {
  return children;
}
